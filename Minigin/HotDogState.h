
//NO STATIC STATES SINCE THERE ARE MULTIPLE INSTANCES OF THIS IN THE GAME!

//class HotDogState //is in(terface. (look how to connect everything (component? need to call update in actual update too!))
//{
//public:
//	//virtual ~HotDogState() {}
//	//virtual void Update(HotDogState& hotdog) {}
//	//virtual HotDogState* GetCurrentState() {};
//	//virtual void handleInput(HotDogState& hotdog, Input input) {} //since it isn't a playable character we don't need a input handles (do use it for player!)
//};